{
  "installCommand": "npm install --prefix saas",
  "buildCommand": "npm run build --prefix saas",
  "outputDirectory": "saas/dist",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
